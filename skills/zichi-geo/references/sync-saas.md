# sync-saas — 同步 SaaS（Part B）

> 状态：占位，Part B 尚未实现。

## 目标

把本地已确认或已发布的只读业务结果同步到客户门户；云端不重新清洗企业资料。

## 计划范围

- 同步企业事实快照、诊断报告、场景库版本、文章元数据和发布回执。
- 使用 `app_id` 关联客户；Token 只进环境变量/密管。
- 支持增量、幂等、失败重试和同步审计。
- 客户门户默认只读展示知识库、诊断、场景、发文和周报。

未定义正式 API、权限和冲突策略前，不执行写入云端的动作。
